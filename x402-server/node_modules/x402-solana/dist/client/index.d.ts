import { X402ClientConfig } from '../types/index.js';
export { WalletAdapter } from '../types/index.js';
import 'x402/types';
import '../x402-protocol-0Omt1wFY.js';
import 'zod';
import '@solana/web3.js';

/**
 * x402 Solana Client
 * Handles automatic payment for x402-protected endpoints
 */
declare class X402Client {
    private paymentFetch;
    constructor(config: X402ClientConfig);
    /**
     * Make a fetch request with automatic x402 payment handling.
     * If a customFetch was provided in the config, it will be used for all requests.
     */
    fetch(input: RequestInfo, init?: RequestInit): Promise<Response>;
}
/**
 * Create an x402 client instance
 */
declare function createX402Client(config: X402ClientConfig): X402Client;

export { X402Client, X402ClientConfig, createX402Client };
